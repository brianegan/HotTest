// HotTest Tests

// Requires
var vows = require('vows'),
    assert = require('assert');

var suite = vows.describe('Hot Test Unit Test Runner');

suite.addBatch({
  'Should Parse Arguments': {
    
  }
});
